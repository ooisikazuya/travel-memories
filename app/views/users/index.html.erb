<div class="container">
  <h1 class="bg-warning text-white text-center">グループメンバー一覧</h1>
</div><br> 
<div class="container">
  <div class="row"> 
    <div class="col-8 d-flex align-items-center">
      <h3 class="text-info">プロフィール画像の設定・変更はこちらから➡︎</h3><br>
    </div>
    <div class="col-4">
      <%= link_to "会員情報の変更", edit_user_path, class: 'btn btn-success btn-lg float-right' %>
    </div>
  </div><br>
  <div class="row"> 
    <div class="col-12">
      <div class="card">
        <div class="card-body">
          <table class="table">
            <tbody>
              <% @users.each do |user|%>
                <tr>
                  <td class="h5 text-info"><%= user.nickname %></td>
                  <% if user.image? %>
                    <td>
                      <%= image_tag user.image.url %>
                    </td>
                  <% else %>
                    <td class="text-primary">画像が選択されていません</td>
                  <% end %>
                </tr>
              <% end %>            
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div> 
</div><br><br>